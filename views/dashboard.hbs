<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rajasthan Police Dashboard</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0; 
            padding: 0;
            background-color: #f4f4f4;
        }

        .dashboard {
            background-color: #1a237e;
            color: #fff;
            text-align: center;
            padding: 20px;
        }

        .tabs {
            background-color: #e53935;
            overflow: hidden;
            display: flex;
            justify-content: space-around;
        }

        .tab {
            padding: 14px 16px;
            text-decoration: none;
            color: #fff;
            font-size: 18px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .tab:hover {
            background-color: #d32f2f;
        }

        .content {
            padding: 20px;
        }

        .statistics {
            background-color: #fff;
            padding: 20px;
            margin-top: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
    </style>
</head>
<body>

    {{#if success}}
    <div class="alert alert-success">
        {{success}}
    </div>
    {{/if}}
    
    <!-- Display error flash message -->
    {{#if error}}
    <div class="alert alert-danger">
        {{error}}
    </div>
    {{/if}}

<div class="dashboard">
    <h1>Rajasthan Police Dashboard</h1>
</div>

<div class="tabs">
    <a href="/complainform" class="tab" onclick="showTab('fileComplaint')"><i class="fas fa-bullhorn"></i> File Complaint</a>
    <a href="#" class="tab" onclick="showTab('history')"><i class="fas fa-history"></i> History</a>
    <a href="#" class="tab" onclick="showTab('logout')"><i class="fas fa-sign-out-alt"></i> Log Out</a>
</div>

<div class="content">
    <!-- Content for File Complaint Tab -->
    <div id="fileComplaint" style="display: none;">
        <h2>File Complaint</h2>
        <!-- Add form or content for filing a complaint here -->
    </div>

    <!-- Content for History Tab -->
    <div id="history" style="display: none;">
        <h2>History</h2>
        <!-- Add content for viewing complaint history here -->
    </div>

    <!-- Content for Logout Tab -->
    <div id="logout" style="display: none;">
        <h2>Log Out</h2>
        <!-- Add content for logging out here -->
    </div>
</div>

<div class="statistics">
    <h2>Complaint Statistics</h2>
    <!-- Add statistical representations of complaints here -->
</div>
</body>
</html>

